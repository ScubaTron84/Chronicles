using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpawnManager : MonoBehaviour
{
    //Clone the spawn manager file from prototype 3.  
    //add your first couple of monster spawns.
    //Give them random path movement from the nav mesh tutorials
    //If the player bumps into them, the player takes a health hit.
    // LEVERAGE YOUR EXISTING PLAYER COLLISION MANAGER
    //

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
